# Loaded automatically by `vite build` (production mode only — local `npm run dev`
# is unaffected). Points the deployed static app at the standalone Functions API,
# since this SWA is on the Free tier and can't use a linked backend to reverse-proxy
# same-origin /api/* requests.
VITE_API_BASE=https://func-rampcast-prod-akdubqa8gmeaanej.westus2-01.azurewebsites.net/
